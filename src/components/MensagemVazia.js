import React from 'react';

const MensagemVazia = () => (
  <p style={{ textAlign: 'center', color: '#8e3b56', marginTop: 20, fontStyle: 'italic' }}>
    Nenhuma tarefa para exibir.
  </p>
);

export default MensagemVazia;